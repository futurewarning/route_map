{
  "name": "route_map",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^3.0.3"
  },
  "dependencies": {
    "arc": "^0.1.4",
    "ol": "latest",
    "requirejs": "^2.3.6"
  }
}
